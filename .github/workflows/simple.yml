name: Simple Usage

on: push

jobs:
  convert_via_latex_pandoc:
    runs-on: ubuntu-18.04
    steps:
      - run: mkdir output        
      - name: Checkout code 
        uses: actions/checkout@v2
      - name: Pandox with Latex
      - uses: Ender-events/pandoc@v1
        with:
          args: '-s -o t3.md book.tex'         
      - uses: action/upload-artifact@master
        with: 
          name: t3.md
          path: output
                    
      - uses: Ender-events/pandoc@master
        with:
          args: '-s -o t3.html book.tex'
          
      - uses: action/upload-artifact@master
        with: 
          name: t3.html
          path: output
            
